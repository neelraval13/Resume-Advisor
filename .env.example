# API endpoint — your Render deployment, or http://localhost:8000 for local backend
VITE_API_URL=http://localhost:8000

# Shared secret for X-API-Key header. Required when calling the deployed backend.
# Leave empty for local dev where backend auth is disabled.
VITE_API_KEY=
